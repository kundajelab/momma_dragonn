Labels were generated by taking the labels from the classification task and running:

zcat labels.txt.gz | perl -lane 'if ($. == 1) {print $_} else {print $F[0]."\t".($F[1]+0.5*(rand(1)-0.5))."\t".($F[2]+0.5*(rand(2)-0.5))}' | gzip -c > regression_labels.txt.gz
